spring:
  application:
    name: aidemo
  ai:
    ollama:
      chat:
        option:
          model: llama2
      base-url: http://localhost:11434/

server:
  port: 8080
  servlet:
    context-path: /aidemo
